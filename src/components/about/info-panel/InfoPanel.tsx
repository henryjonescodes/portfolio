import React from "react";
import Styles from "./info-panel.module.scss";

const InfoPanel = () => {
  return (
    <div className={Styles.infoPanel}>
      <div className={Styles.infoPanel__container}>DIV</div>
    </div>
  );
};

export default InfoPanel;
